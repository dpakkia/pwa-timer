// src/workout-main.tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import WorkoutPage from './Riscaldamento.tsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WorkoutPage />
  </React.StrictMode>
);
